This is a large multi-part redesign of Gari. Read this entire prompt before making any changes, then execute section by section in order.
ABSOLUTE RULES THAT OVERRIDE EVERYTHING ELSE:

Do not break existing working functionality. The following must work identically after every section: user sign up and sign in, VIN scanning and manual VIN entry with NHTSA lookup, guest onboarding flow, document upload to Supabase Storage, document viewing and deletion, the existing document parser confirmation flow, manual expense entry, receipt scanning with Tesseract, monthly expense totals, CarGPT chat with its 20-per-day limit and localStorage history, mechanic save/edit/delete with call button, roadside assistance save/edit/delete with call button, diagnostics issue logging with Supabase persistence, maintenance status derived from expense history, the health score calculation, vehicle switching, and the five-page swipe navigation with scroll snap.
After completing EACH section: run the typecheck. If new errors exist, fix them before starting the next section. Do not accumulate errors.
If a requested change conflicts with existing functionality, implement the safest interpretation and note the conflict in your final summary rather than making a destructive choice.
Never delete or rename Supabase tables, API functions in src/lib/api/, or context providers (AuthContext, VehicleContext, PreferencesContext).
Commit after each major section so there are rollback points.

SECTION 1 — TYPOGRAPHY SYSTEM (do this with care, it touches every file):
Goal: Rajdhani SemiBold (weight 600) becomes the app-wide default font for ALL text. The word GARI is the single exception — always Rajdhani Bold (weight 700).
Implementation approach — do NOT find-and-replace hundreds of inline styles one by one. Instead: First, ensure the Rajdhani font import includes weights 600 and 700. Second, set a global CSS rule on the app root: font-family Rajdhani with weight 600 as default. Third, since most components use inline fontFamily declarations that override globals, work through the codebase replacing fontFamily values: every instance of DM Sans becomes Rajdhani with fontWeight 600, every existing Rajdhani Bold heading stays Rajdhani at fontWeight 700. Preserve all existing font SIZES and letter spacing exactly — only the family and weight change.
Logo treatment: wherever GARI appears as a wordmark next to the garage icon, the text is Rajdhani Bold sized so the capital letters exactly match the icon's height. Audit every screen where the logo appears (entry screen, auth screens, loading screen, top bars) and make this consistent.
Verify after: the dashboard renders, all five pages render, no typecheck errors.
SECTION 2 — VISUAL CONSISTENCY PASS:
Establish one shared visual language across every screen and bottom sheet: consistent corner radii (cards 14-16px, sheets 22px top corners, buttons 12-14px), consistent border treatment (1.5px solid #D4DDD5 where borders exist), consistent spacing rhythm (20px horizontal page padding, 12-16px gaps between cards), consistent shadow treatment (soft, low-opacity shadows only). Every bottom sheet must have the drag handle bar at top and either a visible close affordance or tap-outside-to-dismiss. Every full-screen overlay (camera views, document viewer) must have an obvious back or close button in a consistent position (top-left X in a 44px circular hit area).
Icons: standardize every icon in the app to the same system — stroke-based SVGs, stroke width 1.8, sized on a 20/24px grid, colored #1F6B2E for interactive icons and #6B7C6D for passive ones. Replace any emoji icons (the parts page currently uses emoji like 📘 🔴 📦 🔧) with matching stroke SVG icons in this system.
Keep the existing palette exactly: backgrounds #FFFFFF and #FAFAF8, sage #F4F7F2, text #0D1C0E, muted #6B7C6D, green #1F6B2E, light green #E8F0E9, border #D4DDD5, success #2D9E4A, warning #E5A020, error #C0392B.
Navigation accessibility: from any page or sheet a user must be able to get back in one obvious tap. Audit every flow for dead ends.
SECTION 3 — VIN RESULTS PAGE:
On the vehicle information page shown after VIN scan or manual entry: Remove the current car icon/graphic entirely. Replace with a minimal car silhouette — a simple single-path outline SVG, stroke #D4DDD5, no fill, subtle, decorative rather than focal. Remove the "View Carfax Report" button and the "View NHTSA Complaints" button completely, including any dead code for them. Keep the grouped vehicle data sections (Vehicle Identity, Engine and Drivetrain, Manufacturing, Safety Features) and the recalls section exactly as they are.
At the bottom of this information page add a "Personalize" section with three optional inputs: Nickname (text input, placeholder "Give your car a name"), Color (a row of 8-10 tappable color dots: black, white, silver, grey, red, blue, green, gold/beige, plus a "custom" option), License Plate (text input with uppercase display). These persist to the vehicle record in Supabase for authenticated users or to the guest session for guests. If nickname is left empty, everywhere in the app the car displays as "Your" + model name, e.g. "Your Corolla". If model is also unavailable, "Your Car".
SECTION 4 — VEHICLE CUSTOMIZATION ACCESS:
Users must be able to do all of the following after initial setup, from an obvious place: switch the active vehicle when they have more than one (the existing switcher stays, ensure it's discoverable), update mileage (tappable odometer on the dashboard opens a small sheet with a numeric input prefilled with the current value, validates it's not lower than current, saves to Supabase), and edit nickname, color, and license plate (in the existing Edit Vehicle flow in Settings — ensure color is added there too). All edits reflect immediately in the UI without requiring an app reload.