This is a large multi-part redesign of Gari. Read this entire prompt before making any changes, then execute section by section in order.
ABSOLUTE RULES THAT OVERRIDE EVERYTHING ELSE:

Do not break existing working functionality. The following must work identically after every section: user sign up and sign in, VIN scanning and manual VIN entry with NHTSA lookup, guest onboarding flow, document upload to Supabase Storage, document viewing and deletion, the existing document parser confirmation flow, manual expense entry, receipt scanning with Tesseract, monthly expense totals, CarGPT chat with its 20-per-day limit and localStorage history, mechanic save/edit/delete with call button, roadside assistance save/edit/delete with call button, diagnostics issue logging with Supabase persistence, maintenance status derived from expense history, the health score calculation, vehicle switching, and the five-page swipe navigation with scroll snap.
After completing EACH section: run the typecheck. If new errors exist, fix them before starting the next section. Do not accumulate errors.
If a requested change conflicts with existing functionality, implement the safest interpretation and note the conflict in your final summary rather than making a destructive choice.
Never delete or rename Supabase tables, API functions in src/lib/api/, or context providers (AuthContext, VehicleContext, PreferencesContext).
Commit after each major section so there are rollback points.

SECTION 5 — SMART SCAN, RULE-BASED PARSER (build on what exists, do not duplicate):
The project already has src/lib/documentParser.ts, a ParsedDocumentSheet confirmation component, and an applyParsedActions function in Dashboard.tsx. Improve and complete this existing system rather than creating a parallel one.
Requirements: All parsing is rule-based JavaScript with on-device Tesseract.js OCR. Zero AI API calls in this flow. Document type detection via keyword scoring (invoice, insurance, registration, receipt, unknown) — if multiple types match, highest keyword count wins. Field extraction: dates in formats DD/MM/YYYY, MM/DD/YYYY, YYYY-MM-DD, and written month formats; dollar amounts with the largest value near total-keywords preferred; merchant/shop name from the first non-numeric, non-address line; policy numbers as 8-15 char alphanumerics near "policy"; plate numbers in Canadian formats near "plate"/"permit".
Routing on confirmation: mechanic invoice creates a maintenance expense plus a service-based reminder when oil change is detected; insurance sets 30-day and 7-day expiry reminders; registration fills the plate if missing and sets a renewal reminder; receipt creates a categorized expense (Fuel/Maintenance/Parts/Other guessed from merchant keywords). Unknown documents always save as general documents — never lose a document because parsing failed.
The confirmation card shows: detected type badge, every extracted field as an editable row, a "what will happen" action list, Confirm and Save button, and a "skip automatic actions" link. If OCR extracts no text, skip parsing silently and save the document normally with a brief toast.
Make this parser reachable from the floating smart scan button and from the documents page add flow, with identical behavior in both.
SECTION 6 — MARKETPLACE / PARTS PAGE:
Build a functional parts search experience using stored vehicle data. Layout: search input at top (placeholder "Search parts for your [year] [make] [model]"), category filter chips below (All, Engine, Brakes, Suspension, Electrical, Body, Tires), then a sources list. Each source row (Facebook Marketplace, Kijiji, Amazon.ca, Canadian Tire) shows the source name, an SVG icon matching the app icon system, and a live preview of the query that will be searched ("front brake pads · 2018 Honda Civic"). Tapping opens the source in a new tab with the query URL-encoded: Facebook https://www.facebook.com/marketplace/search/?query=..., Kijiji https://www.kijiji.ca/b-autos-vehicules/... search format, Amazon https://www.amazon.ca/s?k=..., Canadian Tire https://www.canadiantire.ca/en/search.html#q=.... Selected category chip appends its keyword to the query. The search must feel instant — no loading states needed since these are external links. Note at the bottom: "eBay integration coming soon."
SECTION 7 — CAR-GPT UNIFIED AI ASSISTANT:
Keep CarGPT as the single AI assistant — no separate feature. Two improvements. First, accessibility: CarGPT should be reachable from every page, not just the dashboard tile — add it as an action in the floating button options OR a persistent small icon in a consistent screen position, whichever fits the design more cleanly. Second, richer context: when sending a question to the backend, include in vehicleContext everything already sent today PLUS document metadata (type, upload date, and parsed fields where available for the 10 most recent documents), the 20 most recent expenses, all unresolved diagnostic issues, and the current maintenance statuses. The backend Gemini call includes this so users can ask about a specific bill, what they spent in a month, an uploaded document, their car's open issues, or general car questions. Keep the 20 questions per day limit and the localStorage history exactly as they work now. Do not send raw document images — metadata and parsed fields only.
SECTION 8 — DOCUMENTS PAGE REDESIGN:
Reduce visual clutter: each category row gets a small stroke icon from the icon system (shield for insurance, card for license, file for ownership, etc.), the category label, and a right-aligned status — either the latest date, a count pill when more than one file exists, or a subtle "Add" affordance when empty. Increase row height slightly for touch comfort, soften the dividers, and ensure the add-document, view, replace, and delete flows all still work identically. The full-screen viewer keeps pinch zoom and gains nothing new — just ensure its close and action buttons match the standardized icon system.
SECTION 9 — PERFORMANCE AND POLISH:
Lazy-load Tesseract.js and any scanner libraries so they don't load until a scan flow opens. Audit for unnecessary re-renders in the swipe container (memoize page components if needed). Every data fetch shows a loading state — no blank areas while Supabase responds. No layout shift when data arrives — use skeletons or fixed-height placeholders. Page swipe and sheet animations at 60fps — use transform-based animations only, never animating layout properties. Images and the logo properly sized. Remove any console errors that appear during normal use.
FINAL STEP:
Run the full typecheck one last time, fix anything outstanding, then provide a summary listing: every section completed, any conflicts encountered and how they were resolved, any items intentionally skipped and why, and any pre-existing issues you noticed but didn't touch.