Before adding any new features I need to fix several existing issues in the codebase.
Fix 1 — Persist diagnostics to Supabase:
The DiagnosticsPage currently uses local useState which means all issues and maintenance data disappears when the app is closed. Create a new Supabase table called diagnostics with these columns: id uuid primary key default gen_random_uuid(), user_id uuid, vehicle_id uuid, type text, title text, description text, severity text, status text, date_noticed date, date_resolved date, created_at timestamptz default now(). Add RLS policies so authenticated users can only access their own rows. Replace all local useState in DiagnosticsPage with Supabase queries. Issues and maintenance items must persist between sessions.
Fix 2 — Remove dead code:
Delete or clearly mark as unused: the orphaned AddExpenseModal and ExpenseList in the finance folder since Dashboard has its own inline versions. Remove the unused sidebar.tsx file. Fix sonner.tsx by removing the next-themes import and replacing it with a simple theme check or removing theme dependency entirely. Remove or repurpose Header.tsx since Dashboard doesn’t use AppShell.
Fix 3 — Break up Dashboard.tsx:
The current Dashboard.tsx is approximately 3500 lines and contains everything. This needs to be split into separate files for maintainability. Extract these into their own files: ScanReceiptFlow into components/finance/ScanReceiptFlow.tsx. SettingsSheet into components/settings/SettingsSheet.tsx. CategoryPickerSheet into components/finance/CategoryPickerSheet.tsx. CategoryDocumentsListSheet into components/documents/CategoryDocumentsListSheet.tsx. The main Dashboard.tsx should import these components rather than defining them inline. Keep the five page structure and scroll snap navigation exactly as is — do not change any navigation or UI behaviour, only extract components into separate files.
Fix 4 — Wire up Parts page:
The PartsPage is fully stubbed. Add the external source buttons for Facebook Marketplace, Kijiji, Amazon, and Canadian Tire that open pre-filled search URLs using the vehicle’s make, model, and year from VehicleContext. The URL format: Facebook — https://www.facebook.com/marketplace/search/?query=[make]+[model]+[year]+parts. Kijiji — https://www.kijiji.ca/b-cars-vehicles/[make]+[model]+[year]/k0c27l0. Canadian Tire — https://www.canadiantire.ca/en/search.html#q=[make]+[year]+[model]+parts. Amazon — https://www.amazon.ca/s?k=[make]+[model]+[year]+parts. Below the external buttons show a note in DM Sans 13px muted color: “eBay listings coming soon — parts specific to your [year] [make] [model].”
Do these four fixes first before any new features. Show me the result when done.